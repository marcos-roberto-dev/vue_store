// This is just an example,
// so you can safely delete all default props below

import { APPi18N_EN } from './app.i18n.en';

export default {
  failed: 'Action failed',
  success: 'Action was successful',
  ...APPi18N_EN,
};
